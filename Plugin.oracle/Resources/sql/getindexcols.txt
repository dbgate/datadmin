SELECT ux.INDEX_NAME, ux.COLUMN_NAME, ux.COLUMN_POSITION, u.USERNAME FROM USER_IND_COLUMNS ux, USER_USERS u
WHERE NOT EXISTS(SELECT * FROM USER_CONSTRAINTS uc WHERE uc.CONSTRAINT_NAME = ux.INDEX_NAME)
